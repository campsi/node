#collection
  background-image linear-gradient(200deg, rgba(#FFF, .1), rgba(darken(#ebebeb, 10), .6)), url(/images/ui/furley_bg.png)
  > .content
    padding-top $headerHeight

  .form_field
    padding 1em
    padding-bottom 0

    &.big-title
      padding 0

  .component.button
    light-button $theme

  .form_field.fields
    margin-top 1em
    padding-bottom 8em
    background linear-gradient(160deg, rgba(#BBB, .5), 15%, rgba(#EEE, .5))
    > .label
      color black
      font-size 2em
      margin-bottom .5em
    .form_field
      padding 0