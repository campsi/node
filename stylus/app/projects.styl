#projects

  text-align center

  .content
    padding auto 2em

  //& > header a.back
  //  display none
  //
  //&.main > header a.back
  //  display inline-block

  .array_item button.remove
    display none

  .newItem
    input[type=submit]
      display none

  .campsi_project-list
    text-align center

  .items
    display inline-block

  .array_item, .newItem
    display inline-block
    margin 1em

  .campsi_project-list_project
    display inline-block
    text-align: center
    cursor pointer
    width 10rem
    margin 0 auto
    text-decoration none
    color white
    text-shadow 0 1px 1px #000
    position relative

    &.demo .logo::after
      content: 'demo'
      background radial-gradient(farthest-side ellipse at top left, #F96E44, darken(red, 10))
      box-shadow rgba(0,0,0,0.7) 0 1px 2px
      color white
      font-size .6em
      font-weight 700
      text-shadow rgba(black, .8) 0 1px 1px
      position absolute
      display block
      width 4em
      height 4em
      line-height 4em
      border-radius 4em
      top -5px
      right -5px
      border 1px solid #C30000

    &:hover
      .wrapper .logo
        //border-color darken($dark, 6)
        background-color $dark
        border 1px solid lighten($dark, 20)
        transform scaleX(1.05) scaleY(1.05)

    &.active .wrapper .logo, &.active:hover .wrapper .logo
      //border darken($light, 20) 3px solid
      background-color $dark
      border 1px solid lighten($dark, 30)
      transform scaleX(1.1) scaleY(1.1)

    .title
      text-align center
      display block
      margin-top .5em

    &.placeholder

      opacity .5
      transition opacity .1s

      &:hover
        opacity 1

      .name
        font-style italic

      .logo
        border 1px dashed darken($light, 20)
        box-shadow none
        color $theme
        /*font-size em*/
        text-align center



    .wrapper
      display: inline-block;
      position: relative;
      width: 100%;

      .spacer
        margin-top: 100%;

      .logo
        position absolute
        top 0
        bottom 0
        left 0
        right 0


        box-sizing content-box
        background-color rgba(#000, 0.4)
        background-size 80%
        background-clip content-box
        background-position: 50% 50%
        background-repeat no-repeat
        background-image url(/images/ui/new-project.png)
        background-image url(/images/Shop.png)
        /*box-shadow -1px 1px 2px rgba($dark, .1)*/
        border 1px solid $dark

        //box-shadow 1px 2px 5px 0 rgba($dark, .1)
        border-radius 10px //($logoSize / 6)
        //padding $logoPadding
        overflow visible
        display inline-block

        vertical-align middle
        //transition box-shadow .1s ease-in, transform .1s ease-out
        transition transform .1s ease-out, background-color .3s ease-out, box-shadow .1s ease-out

  .items
    display inline
  .newItem
    display inline-block
    .campsi_project-list_project
      opacity 0.5
      transition opacity .3s ease-out
      &:hover
        opacity 1


