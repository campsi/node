light-button($color)

  //background linear-gradient(tint($color, 50), tint($color, 30))
  background tint($color, 50)
  color white
  border-bottom-color darken($color, 20)
  text-shadow 0 1px 1px rgba(black, .5)
  //box-shadow 0 1px 1px 0 rgba(darken($color, 30), .9)
  border 1px solid tint($color, 20)

  &:hover
    //background linear-gradient(tint($color, 60), tint($color, 50))
    background tint($color, 60)
    border 1px solid tint($color, 50)

  &:focus
    box-shadow 0 1px 2px 0 rgba(darken($color, 30), .4)
    //background linear-gradient(tint($color, 60), tint($color, 50))
    background tint($color, 60)

  &:active, &.active
    box-shadow inset darken($color, 20) 0 1px 1px
    border-bottom none


dark-button($color)

  //background linear-gradient(tint($color, 30), tint($color, 10))
  background $color
  //box-shadow 0 1px 1px 0 rgba(darken($color, 50), .5)
  border 1px solid $color

  &:hover
    color lighten($color, 50)
    border-color darken($color, 10)
    //background linear-gradient(tint($color, 40), tint($color, 10))
    background tint($color, 10)
  &:focus
    box-shadow 0 1px 2px 0 rgba(darken($color, 30), .4)
    //background linear-gradient(tint($color, 40), tint($color, 10))
    background tint($color, 10)

  &:active, &.active
    //box-shadow inset darken($color, 30) 0 2px 2px
    background tint($color, 20)


button, a.btn, .buttons > a, input[type=submit]
  &:not(.link)
    display inline-block
    -webkit-appearance none
    text-decoration none
    text-align center
    outline none
    border-radius 4px
    cursor pointer
    font-size: 0.9em
    padding 0 0.8em
    height 2.4em
    line-height 2.4em
    font-weight 500
    border none
    color white
    light-button($light)


  //&:active, &.active
  //  line-height 2.3em
  //i
  //  opacity .3
  //&:hover i
  //  opacity 1

  span
    margin-left .25em

  @media screen and (max-width: 1000px)
    i
      opacity .9
    span
      display none


.form_field
  position relative
  .label
    font-size .9em
    line-height 1.8em
    color lighten($dark, 15)

    text-shadow 0 1px 0px #FFF

  .help, .errors
    opacity .2
    position relative
    background tint($theme, 80)
    color $theme
    font-style italic
    padding .25em .5em .25em 3em
    margin-top .8em
    border-radius .5em
    font-size .8em
    overflow hidden


    &::before
      text-indent 0
      position absolute
      top 0
      left 0
      background rgba($theme, .5)
      content: '?'
      color white
      display inline-block
      width 2.5em
      height 100%
      text-align center
      line-height 2.5em
      font-weight bold
      border-radius .5em 0 0 .5em
      margin-right 1em

    &.errors
      background rgba($red, .2)
      border-color rgba($red, .1)
      color $red

      &::before
        background rgba(red, .5)
        content: '!'

    &:empty
      display none

  &.horizontal
    flex-wrap wrap
    display flex
    align-items center
    .label
      margin-right 1em
      min-width 8em
    .control
      flex 1
    .help, .errors
      flex 100%

.form_field.focus .help, .form_field.focus .errors
  opacity 1
  &:empty
    display none

.control .label
  font-weight normal
  color darken($light, 60)

/*#app .component.form_field.identifier
  display flex
  background darken($light, 20)
  padding 1em
  font-size 1em
  align-items baseline

  .label
    margin 0 .5em 0 0
    font-size 1em
    font-weight 200
    padding 0
    color #000

  .control
    flex 1
    input
      font-family "Consolas", "Menlo", "Courier", monospace
      font-size .8em
      padding .75em
      padding-bottom .5em

  .help .errors
    display none*/

input.component.text
  padding: 0.5em 0.5em 0.4em 0.5em;
  font-size 0.94em
  width: 100%;
  border-radius 4px
  border: darken($light, 6) solid 1px
  border-bottom-color: darken($light, 10)
  box-shadow: inset transparent 0 1px 5px 1px
  transition box-shadow .2s

input.component.text::placeholder
  padding-top 0
  font-weight 200
  font-style: italic

input.component.text:focus
  outline: none;
  border-color: darken($light, 12)
  box-shadow: inset tint($light, 50%) 0 1px 5px 1px

input.component.text:disabled
  background darken(white, 5)
  color lighten($dark, 20)

.component.draggable.gu-mirror
  opacity 1

//.newItem > input[type=submit]
//  display none

.component.file, .component.file_image

  width 100%
  border darken($light, 5) solid 1px
  background #FFF
  border-bottom-color: darken($light, 6)
  box-shadow: inset rgba(black, .07) 0 1px 4px 1px
  border-radius .4em
  position relative
  overflow hidden
  transition background .2s ease-out

  input[type=submit]
    visibility hidden

  &.has-files
    input[type=submit]
      visibility visible

  .control
    display flex
    align-items center
    padding .5em
    background rgba($dark, .8)

    input[type=file]
      flex 1
      color white
      font-size .9em

  .value
    text-align center
    img
      margin 1em
      max-height 200px
  &.is-dragover
    background $theme


  input[type=submit]
    dark-button $theme

  .progress
    background red
    position: absolute;
    top: 50%;
    left: 50%;
    margin-left: -50px;
    margin-top: -50px;

#app .form_field.big-title
  //background linear-gradient(45deg, #FFF, darken(#FFF, 2))
  //background tint($light, 60)
  //border-bottom 1px solid darken($nearWhite, 14)
  //border-top 1px solid darken($nearWhite, 10)
  font-size 1.5em
  //background: url(/images/ui/low_contrast_linen.png)
  //box-shadow inset black 0 2px 10px 2px
  //margin-bottom .5em


  padding 0
  //border-top 1px solid darken($light, 5);

  .label
    font-weight 600
    font-size 1.2em
    padding 1em
    //text-indent 1.5em
    //text-align center
  .control
    position relative
    padding 0

    &::before
      content '✎'
      color lighten($theme, 10)
      width 1.5em
      height 50%
      text-align center
      font-size 1.5em
      line-height 1.2em
      position absolute
      left 0
      top .8em

    input
      background tint($light, 60)
      border none
      outline none
      text-indent 1.5em
      display block
      transition background .2s ease-out
      padding .75em
      font-size: 1.5em
      font-weight lighter
      box-shadow none
      border-radius 0
      //color white
      //text-shadow black 1px 1px 2px
      &:focus
        //box-shadow none;
        background: white
        align-self: center
      //box-shadow: inset #ededed 0 1px 1px;
      &.component.text::placeholder
        padding-top .35em
        font-weight 200
        font-style: italic

.component.text_area
  position relative
  width 100%
  min-height 5rem
  border: darken($light, 10) solid 1px;
  box-shadow: inset rgba(black, .1) 0 1px 1px 1px
  border-radius 4px
  padding .5em
  font-size .90em
  margin 0

  &:focus
    outline: none;
    border-color: lighten($theme, 20)
    box-shadow: inset lighten($theme, 20) 0 0 2px;


#app .form_field.url

  .label, .control
    padding 0
    display inline-block
    margin 0 1em 0 0
    text-overflow ellipsis
    white-space: nowrap;
    overflow: hidden;
    a
      font-family Menlo, Monaco, Consolas, "Lucida Console", monospace
      font-size .8em
      color lighten($dark, 10)
      font-weight 100

select.select_dropdown
  width 100%

.number_rating
  user-select none
  span
    user-select none
    display inline-block
    height 1.5em
    width 1.5em
    opacity .3
    text-align center
    cursor pointer

    &:last-child
      margin 0

    &.checked
      opacity 1



.campsi_help-tour-step
  display inline-block
  display none
  background rgba($theme, 0.2)
  color lighten($theme, 20)
  //text-shadow 0 1px 1px $theme
  margin 1.5em
  border-radius .5em
  border 1px solid rgba($theme, 0.4)
  border-bottom-width 2px
  position relative
  max-width 50em
  opacity 1
  //
  //&.hidden
  //
  //  animation-name fadeOut
  //  animation-duration 5s
  //  animation-fill-mode forwards

  .counter
    position absolute
    bottom 0.25em
    right .5em
    color rgba($theme, .6)
    font-weight bold
    font-size 1.5em
  button
    box-shadow none
    background transparent
    border none
    position absolute
    padding 0
    margin 0
    right .5em
    top .5em
    color $theme
  .message
    padding 1em 2em 1em 1em
    text-align left
    p:last-child
      margin-bottom 0


.panel.dark > .content .fields


  .form_field
    padding 1em
    border-bottom 1px solid darken($dark, 5)

  .label
    color transparentify(white, $dark, .7)
    font-weight 200
    margin-bottom .3em
    font-size 1.1em

  form > .form > .fields > .form_field > .label
    font-weight 500
    font-size 1.2em
    padding .5em 0

  input.text, textarea
    background rgba(#FFF, .1);
    border 1px solid rgba(#FFF, .2);
    border-radius .5em
    color white
    &:focus
      border 1px solid rgba(#FFF, .6);
      box-shadow none



  form
    font-size .9em
    button
      dark-button $theme


  .form_field[data-comp-type='checkbox']
    .label
      display none

    .checkbox label
    span
      color transparentify(white, $dark, .7)

  h3
    margin-top 2em
    font-style italic
    font-weight 200

  .array_item, .newItem
    color white
    background-color $dark
    border 1px solid lighten($dark, 30)
    padding .5em
    margin-bottom 1em
    border-radius .5em
    position relative
    vertical-align middle
    min-height 6.5em


intro()
  border-radius .3em
  box-shadow 0 0 30px lighten($dark, 70)
  padding 1.5em
  margin 1em .5em
  display block
  background white
  font-size 1.2em
  font-weight 200
  line-height 1.8em
  color lighten($dark, 30)

  h3
    margin-top 0
  &.hidden
    display none

  ul
    margin-left 1em

  a
    font-weight 600