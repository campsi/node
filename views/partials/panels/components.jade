mixin component(name, label, icon)
  .component.draggable.icon(data-component-type=name)
    div.icon.drag-handle=icon
    span.name=label

.search
  form
    input(type="search", placeholder="search")

.standard
  //h2 Standard
  .group
    h3 Text & Number
    .components.dragzone
      +component('text', 'Text Field', 'A')
      +component('text-area', 'Text Area', 'Abc')
      +component('number', 'Number', '0-9')
      +component('checkbox', 'Checkbox', '☑')
      .clear
  .group
    h3 Select & groups
    .components.dragzone
      +component('select', 'Dropdown', '')
      +component('select-ajax', 'Ajax Select', '')
      +component('radio-button-group', 'Radio Button Group', '')
      +component('combobox', 'Combo Box', '')
      .clear
  .group
    h3 Structure
    .components.dragzone
      +component('array', 'Array', '')
      +component('form', 'Form', '')
      .clear